node {

    stage('checkout') { // for display purposes
       
    }
    stage('install packages') {

    echo "install packages"
        
    }
    stage('test') {

    echo "running tests.."
        
    }
}
